---
bump: minor
category: Features
---

`hop <expr>` can open a repository's default-branch checkout. `hop flipbook default` jumps to it without naming the branch, and `hop flipbook` lists it first. Each word of a query now narrows the results, so `hop flipbook m` finds `master`, and remaining matches are ordered by most recent use.
